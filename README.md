Simple ANSI terminal emulator, based on Curses library.

# Prerequisites

On Linux:

    sudo apt-get install libncurses-dev

On MacOS:

    brew install ncurses

# Build

Compile the terminal emulator from sources and install into /usr/local:

    make
    make install

Run tests:

    make test
